__DOTFILES_DIR="$(cd $(dirname ${BASH_SOURCE:-$0}); pwd)"

# .bashenv.d を読む
for s in $(ls $__DOTFILES_DIR/.bashenv.d); do
    source $__DOTFILES_DIR/.bashenv.d/$s
done

export PATH=$HOME/.local/bin:$PATH
